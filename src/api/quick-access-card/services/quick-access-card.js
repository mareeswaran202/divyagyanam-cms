'use strict';

/**
 * quick-access-card service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::quick-access-card.quick-access-card');
