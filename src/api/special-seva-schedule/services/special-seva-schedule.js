'use strict';

/**
 * special-seva-schedule service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::special-seva-schedule.special-seva-schedule');
