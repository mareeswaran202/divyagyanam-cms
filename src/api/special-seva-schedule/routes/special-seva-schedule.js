'use strict';

/**
 * special-seva-schedule router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::special-seva-schedule.special-seva-schedule');
